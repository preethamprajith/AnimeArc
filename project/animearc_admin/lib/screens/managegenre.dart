import 'package:flutter/material.dart';

class Managegenre extends StatelessWidget {
  const Managegenre({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("manage genre"),
    );
  }
}