import 'package:flutter/material.dart';

class Viewscomplaints extends StatelessWidget {
  const Viewscomplaints({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("views and complaints"),
    );
  }
}