import 'package:flutter/material.dart';

class Manageanime extends StatelessWidget {
  const Manageanime({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("manage anime"),
    );
  }
}